<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.coinvs.testsurfaceview">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ShowActivity1" />
        <activity android:name=".showActivity2" />
        <activity android:name=".show712" />
        <activity android:name=".BezierActivity" />
        <activity android:name=".AnimationActivity" />
        <activity android:name=".CoordinateLayoutActivity" />
        <activity android:name=".ReboundActivity" />
        <activity android:name=".Book1_1Activity"></activity>
    </application>

</manifest>